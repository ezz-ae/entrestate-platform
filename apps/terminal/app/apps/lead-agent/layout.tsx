import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Insta DM Lead Agent - Entrestate",
  description:
    "Qualify real estate leads inside Instagram DMs, websites, QR codes, and landing pages.",
}

export default function LeadAgentLayout({ children }: { children: React.ReactNode }) {
  return children
}
